export default class QualificationSchema {

  qualifyAndSerializeSchema(qualification) {
    // Empty by default.
  }

  deserializeSchema(schema, schemaResult) {
    // Empty by default.
  }

  render(parameters) {
    // Empty by default.
  }
}
